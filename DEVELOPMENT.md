# 开发记录

## 2026-09-16 — Obsidian Companion Phase 6：发布候选自动化

本地发布流程加入 Companion 构建，并生成 Chrome 扩展、Obsidian Companion 和无 Git 历史源码三个 ZIP。发布源码白名单纳入 `packages/`，隐私扫描同时覆盖 Companion 源码及构建产物。自动化部分执行完整测试、TypeScript、双端生产构建、入口检查、隐私扫描、生产依赖审计和包内容核验；真实 Chrome/Obsidian 权限提示、应用重启、实际 Vault 写入和迁移仍须人工验收。

## 2026-09-16 — Obsidian Companion Phase 5：安全迁移

新增普通文件夹到当前 Companion Vault 的预览式迁移。预览逐条比较稳定 ID、原始 Markdown、元数据和附件，区分待导入、完全相同和冲突；执行只导入新收藏，不覆盖冲突内容，不删除或修改来源文件夹。若旧目录本身已对应 Companion 管理的同一批文件，内容会被识别为相同并跳过复制。

协议增加 `library-import-v1` capability 与 `/v1/imports` 领域端点。导入保留原始标题、创建时间、笔记本、Markdown 和附件；客户端使用 `import:<entryId>` 幂等键。服务端用持久化 import pending 记录处理笔记完成但索引提交失败的中断，重试会继续提交；同 ID 不同内容和附件冲突均拒绝覆盖。迁移入口仅在 Companion 模式设置中显示，读取旧目录时仍由用户明确授权。

## 2026-09-16 — Obsidian Companion Phase 4：扩展接入与双后端切换

新增扩展端 Companion 客户端和 `ObsidianLibraryBackend`，覆盖状态、索引、收藏、阅读与图片、建文件夹、改名、移动、移出/撤销和索引重建。统一后端门面在每次操作前读取当前模式，使 MV3 后台休眠恢复后仍使用正确目标；收藏成功写入原有刷新通知，侧栏无需等待轮询。

首次设置优先展示 Obsidian 配对入口，支持配对码和自定义本地端口；设置中可重新配对或切回普通文件夹。连接令牌只保存在 `chrome.storage.local`。Manifest 增加回环地址 `connect-src` 和仅在用户点击配对时申请的 `http://127.0.0.1/*` 可选权限，不申请局域网其他地址或公网主机权限。旧目录与连接信息均保留，切换模式不搬移数据。

## 2026-09-16 — Obsidian Companion Phase 3：服务端插件

新增可独立构建的桌面端 Obsidian 插件。插件只监听 `127.0.0.1`，提供 5 分钟单次配对码、失败次数限制、扩展 Origin 校验、Bearer 令牌哈希存储、协议版本验证、CORS、请求体上限及领域级路由。设置页支持保存目录、端口、生成配对码和撤销全部客户端。

Vault 层实现 Markdown 与 Base64 图片写入、索引、阅读、建文件夹、改名、移动、隐藏/撤销和索引重建。保存使用持久化 pending 记录，在笔记完成但索引提交失败后可安全重试；读取外部修改的索引前重新验证路径和字段。插件构建产物为 `packages/obsidian-plugin/main.js`。

新增本地服务器、认证、幂等键、内存 Vault、一致性恢复和恶意索引测试。扩展配对 UI 尚未接入，将在 Phase 4 完成。

## 2026-09-16 — Obsidian Companion Phase 2：本地协议 v1

冻结扩展与 Companion 的首版 HTTP/JSON 契约：仅监听回环地址、短时配对码与 Bearer 令牌、显式协议版本和 capability、统一成功/错误信封、领域级端点、收藏幂等键、请求体与图片上限、错误码及重试语义。新增客户端和服务端可共用的 TypeScript 类型与不可信数据解析器，拒绝错误版本、未知 capability、伪造错误码、非法路径、图片列表不一致及超限收藏。

协议说明见 [COMPANION_PROTOCOL.md](docs/COMPANION_PROTOCOL.md)。209 项测试、TypeScript、生产构建及扩展入口检查通过。Phase 2 只定义协议，不开放本地端口，也不增加扩展权限。

## 2026-09-16 — Obsidian Companion Phase 1：笔记库后端抽象

新增统一 `LibraryBackend` 契约和现有目录存储的 `DirectoryLibraryBackend` 适配器。后台收藏、列表、正文与附件读取、卡片预览、建文件夹、改名、移动、移出与撤销、索引修复均改为通过后端调用；目录选择与首次可写性探测仍保留为目录后端专属设置流程。磁盘格式、索引格式及用户行为不变，为后续增加 Obsidian 本地接收后端保留替换点。

补充后端状态、列表、正文和有界预览测试。203 项测试、TypeScript、生产构建及扩展入口检查通过。

## 2026-09-06 — 新标签页阅读入口

将导出区域的「在独立页面中打开」移至单条收藏的顶部阅读工具栏，改名为「在新标签页阅读」。通过 URL 传递收藏 ID，新页面加载本地数据后直接打开该收藏；独立阅读模式不跟随活动标签页变化，返回列表后可阅读全部收藏。收藏被删除时提示并显示列表，打开失败可重试。独立阅读页面不重复显示新开标签按钮。

113 项现有测试、TypeScript、生产构建及扩展入口检查通过；真实浏览器跳转体验待用户验收。

## 2026-09-06 — 跟随系统明暗模式

阅读侧栏和独立页面通过 `prefers-color-scheme` 自动响应系统外观，无手动主题设置。深色样式覆盖收藏列表、选中状态、阅读正文、工具栏、代码块、表格、链接、表单、错误和危险操作，原生控件启用深色 `color-scheme`。浅色样式保持原样。

TypeScript、生产构建及扩展入口检查通过；计算核验深色主要正文和控件文字配色，对比度均超过 4.5:1。浏览器实际外观由用户切换系统明暗模式验收。

## 2026-09-05 — 阅读与收藏视觉打磨

按已确认方向统一浅色背景、低饱和绿色、按钮与分隔线。收藏标题和摘要各最多两行，日期弱化，批量选中行增加背景反馈。阅读正文调整为 16px（窄屏 15px），优化段落及代码块间距，新增收藏日期和来源标识。定位保留主按钮，复制降为次级按钮，删除保留在更多操作；导出入口移至正文前，与其他操作集中。未新增功能。

TypeScript、生产构建、扩展入口及 diff 格式检查通过。依用户节省用量的偏好，浏览器视觉验收留给用户。

## 2026-09-05 — 产品范围复盘

已整理 [避免过度开发的经验](docs/PRODUCT_LESSONS.md)：当前以收藏、找回、定位和导出为完整流程，优先实际使用与可靠性修复；新增功能以反复出现的不便为依据，历史任务管理规划不自动推进。

## 待开发需求入口

见 [需求记录](docs/BACKLOG.md)。Obsidian 导出需求已获用户开发授权，v0.4 本地实现完成，待手动验收。

## 2026-09-05 — v0.4 Obsidian 导出

新增单条、所选、当前列表 Markdown 导出。目录句柄保存在 IndexedDB，写入前请求目录授权；不新增扩展权限。YAML frontmatter 使用字符串转义，正文由 HTML 转 Markdown，保留标题、列表、表格、链接、代码块；旧收藏回退到保存的文本。稳定文件名使用收藏 ID，已有文件跳过，多窗口导出串行，写入失败中止并清理此次创建的占位文件。测试覆盖 YAML 解析、正文转换、文件命名、已有文件保护、权限与写入失败。

浏览器目录授权、关闭后恢复目录及 Obsidian 属性展示由用户按 [验收步骤](docs/OBSIDIAN_EXPORT.md) 操作，尚未标记实机验收通过。

## 2026-09-05 — Milestone 1

已实现：Manifest V3、工具栏打开侧边栏、当前窗口活动标签页监听、ChatGPT 对话 ID 解析、加载及异常状态、窄屏界面、安装说明。

验证结果：

- `npm test`：19 项测试通过（URL 解析 16 项，标签页监听 3 项）。
- `npm run build`：TypeScript 严格检查、Vite 构建及扩展入口文件校验通过。
- `npm install`：审计 62 个包，0 个已知漏洞。
- Chrome 手动验收尚未完成：首次目录选择误选项目根目录，Chrome 正确报告清单缺失。后续浏览器工具阻止访问 `chrome://extensions`，没有继续绕过限制。需按 README 在 Chrome 中加载 `dist`，验证侧边栏及真实对话切换。

阶段 1 代码已就绪，尚未标记验收完成。阶段 2 未开始。


## Milestone 1 — 用户验收通过

用户确认能够识别对话级 ID，并在切换对话后同步更新。2026-09-05 进入第二阶段。

## Milestone 2 — 代码就绪，待手动验收

已实现项目创建、选择、重命名，自动创建空计划，计划标题修改，步骤创建、编辑、备注、删除确认、上下排序及四种状态。本地保存最近选择的项目；选中步骤仅为 UI 状态，与持久化的当前步骤分离。

领域变更为独立纯函数，Chrome 存储封装在 repository 中。所有写入通过 service worker 排队处理，防止多窗口更新覆盖。写入失败不更改既存数据，队列可继续工作；不支持的数据版本拒绝写入。UI 订阅本地存储变化。

验证：29 项测试通过，覆盖领域规则、重建 repository 后的数据恢复、并发写入、失败恢复以及原有 URL/标签页事件。TypeScript、Vite 和构建入口检查通过。真实 Chrome 刷新及重启验收需用户按 README 操作；尚未进入第三阶段。


## Milestone 2 — 用户验收通过

2026-09-05 用户确认验收通过，并要求进入第三阶段。

## Milestone 3 — 代码就绪，待手动验收

支持关联已有项目、新建项目时原子关联当前对话、显式改绑及保留原数据。根据对话解析项目/计划，不在页面切换时写入全局项目选择，避免不同窗口互相覆盖。进入对话自动选择当前步骤详情；普通点击仍为 UI 状态。

明确规则：同一计划的多个对话共享唯一当前步骤，状态变更同步所有关联记录，删除或完成当前步骤清除指针。返回对话不回滚计划状态。未关联对话显示关联入口，不展示另一项目的步骤。

验证：36 项测试通过；包括创建与绑定原子性、不同对话恢复、同计划共享进度、改绑保留旧数据、错误 URL/归属拒绝、旧记录兼容及新 repository 恢复。TypeScript、生产构建和扩展入口检查通过。真实 Chrome 验收按 README 第三阶段清单执行，尚未进入第四阶段。


## Milestone 3 — 用户验收通过

2026-09-05 用户确认验证通过，并要求进入第四阶段。

## Milestone 4 — 代码就绪，待手动验收

实现步骤下的问题创建、编辑、解决、重新打开、确认删除及新标签页打开关联对话。新问题默认使用当前对话 URL；可修改或清空。问题状态独立于步骤状态。问题操作更新所属步骤、计划和项目时间戳；删除步骤级联清理问题。

沿用串行后台存储，维持现有数据版本及权限。链接写入和导航均校验 ChatGPT 对话 URL，兼容仅含 conversationId 的旧记录。打开失败以简短消息显示，技术细节仅记录到控制台。

验证：45 项测试通过，覆盖问题生命周期、级联删除、所属步骤隔离、父对象时间戳、持久化恢复、链接边界和新标签页导航失败。TypeScript、Vite 生产构建及扩展入口检查通过。真实 Chrome 验收按 README 第四阶段清单执行；第五阶段未开始。


## Milestone 4 — 用户验收通过

2026-09-05 用户确认测试通过。讨论后明确：第五阶段降低计划录入和备份成本，之后还需执行引导阶段完善整体路线、当前位置与下一步行动体验。

## Milestone 5 — 代码就绪，待手动验收

实现窄范围 Markdown 清单解析、追加前预览、显式编号冲突阻止、自动编号和原始文字保留。既有步骤、问题与进度不被导入覆盖。

实现全部本地数据 JSON 导出、文件读取及数量预览、结构与引用完整性校验、用户确认替换，以及后台再次校验。恢复进入原有串行写入队列，确认基于的存储快照不一致时拒绝覆盖；写入失败保留原数据。无新增权限。

验证：57 项测试通过；包含十步清单验收、重复编号与原子追加、完整备份往返、16 类字段/引用损坏、非法文件、过大文件、过期确认、恢复持久化和失败回滚。TypeScript 严格检查、生产构建及扩展入口检查通过。

待用户按 README 在真实 Chrome 中验收下载与恢复。第五阶段还未标记验收完成；执行引导阶段未开始。


## Milestone 6 — 代码就绪，与阶段 5 统一验收

用户明确要求继续阶段 6 后统一验证，因此不将第五阶段提前标记验收通过。

实现固定进度导航、当前序号、下一步、步骤详情和原子推进。推进跳过已完成步骤，保留阻塞状态，回到此前未完成工作；未解决问题需确认。详情支持说明、命令、完成标准，扩展窄范围清单导入与 JSON 校验，保留旧字段兼容性和导入原文。

68 项自动测试通过，TypeScript 与生产构建通过。模拟窄屏界面确认：STEP-08 推进到 STEP-09、完成比例更新、详情编辑保存、查看其他步骤不改变当前位置、下一步阻塞提示及推进后仍保持阻塞。滚动时导航固定在顶部；修正“查看当前操作”只选中不定位的问题，确认详情起点位于导航下方而非被遮住。

新增的统一验收、执行引导和示例现已归档到 `docs/archive/legacy-task-planner/`。真实 Chrome 下载、恢复、重启和真实长任务体验当时待统一验收。

## v0.2 Phase 1–2 — 消息收录与来源回溯

新增独立 ChatGPT 内容脚本，读取消息 ID 和正文，提供预览、计划/步骤选择、新建步骤及主动收录。来源与步骤使用独立 ID、多对多引用，支持重复去重、用途分类、解除关联及显式共享快照更新。侧栏显示适配状态，生成中禁止收录。

新增按对话 ID 导航、消息 ID 精确定位、高亮及有界失败回退，保留文本快照，不依赖消息直链。存储升级至 v2，读取不写盘，成功命令写入时迁移；备份验证与序列化兼容旧版并包含来源与引用。所有写入先校验，避免写入下次无法读取的数据。

独立模拟页面已验证实际点击收录、重复去重、多条归档与定位高亮；真实 Chrome 扩展刷新、跨对话历史加载、重启和备份下载当时计划按归档的 `MESSAGE_ACCEPTANCE.md` 验收。Phase 3 未开始。

## v0.2 Phase 3 — 总目标、阶段与步骤

版本 0.2.1：增加目标与目标完成条件、阶段实体与顺序、步骤归属。迁移至存储 v3，兼容 v1/v2 输入；旧步骤 ID、状态及所有来源引用保留。跨阶段移动追加到目标阶段末尾，阶段排序同步重排执行顺序，当前步骤指针不回滚。

新增保守结构化导入和完整预览、可复制整理提示词；明确目标替换选项，未结构化文字保留原文。进度标明按步骤计数，全部步骤完成后提示核对目标条件。

独立模拟页面验证目标显示、阶段分组、两阶段导入、保留原目标及当前步骤跨阶段移动。真实 Chrome 迁移持久化与备份下载当时计划按归档的 `PHASE3_ACCEPTANCE.md` 执行，未提前标记用户验收通过。

## v0.3 — 简化为回复收藏与定位

依据用户反馈暂停白板与进一步任务管理，主界面替换为收藏列表及原文阅读。收录改为一键收藏，不创建项目或步骤；保留旧数据在备份中。新增已加载长回复查找、结构化原文安全渲染、批量删除、一次确认清空全部数据及过期清空保护。此前 Phase 计划不再作为当前开发方向。

## v0.3 — 复制全文

阅读页增加“复制全文”，直接复制保存的 reply.text；成功反馈两秒，失败允许重试。窄侧栏工具栏允许换行。按用户要求未使用 computer use，仅运行代码检查与构建。

## v0.3 — 阅读器视觉整理

新增 reader.css，覆盖当前简化界面，不改数据模型与交互流程。收藏范围使用分段切换，列表两行标题与单行摘要，移除字符数；阅读页统一字号、行距、标题、列表、表格与代码样式，窄屏工具栏可换行。连接提示与数据入口降低视觉权重，保留键盘焦点与减少动态效果支持。按用户要求未使用 computer use，视觉体验待用户自行验证。

## v0.3.1 — 长对话定位

修复只搜索已挂载 DOM 的限制。内容脚本异步执行有界、重叠视口扫描，识别对话滚动容器，等待历史挂载，精确匹配消息 ID 并确认可见后高亮。用户操作/切换/新定位可中止旧操作；面板仅对连接失败重试，不重复启动完整扫描。新增 8 项查找算法测试。未进行真实网页自动化测试，待用户验收。
